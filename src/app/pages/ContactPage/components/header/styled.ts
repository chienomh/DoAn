import { Box, styled } from '@mui/system';

export const Title = styled(Box)(() => ({
  fontSize: '26px',
}));

export const Content = styled(Box)(() => ({
  fontSize: '18px',
  color: '#595959',
}));
